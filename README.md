# 🧠 AI Idea Generator

Полнофункциональная система генерации инновационных решений на основе ИИ-агентов с использованием Node-RED, React и локальных LLM моделей через Ollama.

## 🎯 Что делает система

Система получает описание проблемы от пользователя и автоматически генерирует оптимальное решение через многоэтапный AI-процесс:

1. **Генерация случайных слов** - создание 6+ разнообразных слов через алгоритм Mersenne Twister
2. **Brainstorming фаза** - ИИ генерирует 5 инновационных идей на основе проблемы и ключевых слов
3. **Критический анализ** - второй ИИ-агент анализирует идеи и синтезирует лучшее решение

## 🚀 Архитектура

### Backend (Node-RED)
```
HTTP API / Dashboard → Init Session → Mersenne Twister → Word Generator AI
                                              ↓
                                          Redis Storage
                                              ↓
                                    Brainstorm AI (5 идей)
                                              ↓
                                      Critic AI (анализ)
                                              ↓
                                    JSON Response / UI Update
```

### Frontend
- **React App** (порт 5173) - современный веб-интерфейс с поддержкой Markdown
- **Node-RED Dashboard** (порт 1880/ui) - встроенный UI для быстрого доступа

## ⚒️ Требования

### Системные компоненты
- **Node.js** 20.18+ и **npm**
- **Docker** для запуска Redis
- **Ollama** для локальных LLM моделей

### AI Модели
```bash
ollama pull gpt-oss:20b-cloud          # Word Generator
ollama pull deepseek-v3.1:671b-cloud   # Brainstorming Agent
ollama pull gpt-oss:120b-cloud         # Critic Agent
```

## 🔧 Установка и запуск

### 1. Клонирование репозитория
```bash
git clone https://github.com/VladTer06081963/ai-idea-generator.git
cd ai-idea-generator
```

### 2. Установка зависимостей

**Backend (Node-RED):**
```bash
npm install
```

**Frontend (React):**
```bash
cd frontend
npm install
cd ..
```

### 3. Запуск Redis
```bash
docker-compose up -d redis
# или
docker run --name redis-brainstorm -p 6379:6379 -d redis:alpine
```

Проверка:
```bash
docker ps | grep redis
```

### 4. Запуск Ollama
```bash
ollama serve
```

### 5. Запуск приложения

**Терминал 1 - Node-RED (Backend):**
```bash
npm start
```
Сервер доступен: `http://localhost:1880`

**Терминал 2 - React (Frontend):**
```bash
cd frontend
npm run dev
```
Приложение доступно: `http://localhost:5173`

## 📡 Использование

### Вариант 1: React Web App (рекомендуется)
1. Откройте `http://localhost:5173`
2. Введите вашу проблему в текстовое поле
3. Нажмите **"Generate Ideas 🚀"**
4. Дождитесь результата (обычно 30-60 секунд)

### Вариант 2: Node-RED Dashboard
1. Откройте `http://localhost:1880/ui`
2. Введите проблему в форму
3. Нажмите **"Brainstorm!"**

### Вариант 3: API
```bash
curl -X POST http://localhost:1880/brainstorm \
  -H "Content-Type: application/json" \
  -d '{"problem": "Как научить кота программировать?"}'
```

**Пример ответа:**
```json
{
  "success": true,
  "finalSolution": "# Окончательное предложение...",
  "originalProblem": "Как научить кота программировать?",
  "timestamp": "2025-11-21T12:00:00.000Z",
  "sessionId": "1732190400000"
}
```

## 🎨 Возможности

### React Frontend
- ✅ Современный, отзывчивый дизайн
- ✅ Поддержка Markdown (включая таблицы через `remark-gfm`)
- ✅ Анимации загрузки
- ✅ Обработка ошибок
- ✅ Отображение метаданных (session ID, timestamp)

### Node-RED Backend
- ✅ HTTP API endpoint (`/brainstorm`)
- ✅ Встроенный Dashboard UI
- ✅ Раздельная маршрутизация для API и Dashboard
- ✅ Redis для временного хранения данных
- ✅ Автоматическая очистка (TTL 30 секунд)
- ✅ Debug nodes для отладки

### AI Pipeline
- ✅ Mersenne Twister для генерации случайных чисел
- ✅ Генерация разнообразных ключевых слов
- ✅ Brainstorming с использованием DeepSeek v3.1
- ✅ Критический анализ с GPT-OSS 120B
- ✅ Markdown форматирование результатов

## ⚙️ Конфигурация

### Порты
- **Node-RED**: 1880
- **React App**: 5173
- **Redis**: 6379
- **Ollama**: 11434

### Настройки AI
Все модели используют `temperature: 1.0` для максимальной креативности.

Для изменения количества генерируемых слов, отредактируйте узел **"Check >= 6 words (TEST MODE)"** в Node-RED flow.

### Proxy настройки (React → Node-RED)
Настроены в `frontend/vite.config.js`:
```javascript
server: {
  proxy: {
    '/brainstorm': 'http://localhost:1880'
  }
}
```

## 🛠️ Разработка

### Структура проекта
```
.
├── .nodered/
│   ├── flows.json          # Node-RED flow конфигурация
│   └── ...
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── IdeaForm.jsx
│   │   │   └── IdeaResult.jsx
│   │   ├── App.jsx
│   │   └── index.css
│   ├── package.json
│   └── vite.config.js
├── docker-compose.yml      # Redis конфигурация
├── settings.js             # Node-RED настройки
└── package.json            # Backend зависимости
```

### Отладка
- **Node-RED Flow**: `http://localhost:1880` → вкладка "AI Idea Generator"
- **React DevTools**: F12 в браузере
- **Redis**: `docker exec -it redis-brainstorm redis-cli`

### Логи
```bash
# Node-RED логи в терминале где запущен npm start
# React логи в терминале где запущен npm run dev
# Redis логи: docker logs redis-brainstorm
```

## 🐛 Troubleshooting

### React сервер "отвалился"
```bash
cd frontend
npm run dev
```

### Redis не запускается
```bash
docker ps -a | grep redis
docker start redis-brainstorm
# или
docker-compose up -d redis
```

### Ollama модели не найдены
```bash
ollama list
ollama pull gpt-oss:20b-cloud
ollama pull deepseek-v3.1:671b-cloud
ollama pull gpt-oss:120b-cloud
```

### Порт 1880 занят
```bash
pkill -f node-red
npm start
```

## 📊 Производительность

- **Генерация слов**: ~5-10 секунд (6 слов)
- **Brainstorming**: ~15-30 секунд (зависит от модели)
- **Критический анализ**: ~15-30 секунд
- **Общее время**: 30-70 секунд

> **Примечание**: В TEST MODE генерируется только 6 слов вместо 36 для ускорения тестирования.

## 🔄 Обновления

### v2.0 (текущая версия)
- ✅ Добавлен React frontend
- ✅ Поддержка Markdown таблиц
- ✅ Раздельная маршрутизация API/Dashboard
- ✅ Улучшенная обработка ошибок
- ✅ Современный UI/UX

### v1.0
- ✅ Базовая Node-RED реализация
- ✅ Redis интеграция
- ✅ Ollama AI pipeline

## 📄 Лицензия

MIT License - свободное использование для любых целей.

## 🤝 Вклад

Pull requests приветствуются! Для крупных изменений, пожалуйста, сначала откройте issue для обсуждения.

## 📧 Контакты

- GitHub: [@VladTer06081963](https://github.com/VladTer06081963)
- Repository: [ai-idea-generator](https://github.com/VladTer06081963/ai-idea-generator)
